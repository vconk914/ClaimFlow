{
  "buildCommand": "pnpm -w exec -- tsc --build && pnpm --filter @workspace/billing-dashboard run build",
  "outputDirectory": "artifacts/billing-dashboard/dist/public",
  "installCommand": "pnpm install --frozen-lockfile"
}
